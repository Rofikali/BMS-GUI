# Example Data Directory

This directory documents the expected MVP file-storage layout. Runtime data should be created under a local `data/` directory, which should not be committed.

The canonical storage contract is `docs/FILE_STORAGE_SPEC.md`.
